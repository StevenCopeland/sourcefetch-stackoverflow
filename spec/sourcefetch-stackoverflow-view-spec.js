'use babel';

import SourcefetchStackoverflowView from '../lib/sourcefetch-stackoverflow-view';

describe('SourcefetchStackoverflowView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
